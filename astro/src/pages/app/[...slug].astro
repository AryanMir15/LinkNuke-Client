---
// BrowserRouter is a React component, so it needs the client:only directive
import { BrowserRouter } from 'react-router-dom';
import App from '../../react-app/App.jsx';

export const prerender = false;

---
<BrowserRouter basename="/app" client:only="react">
  <App client:only="react" />
</BrowserRouter>
